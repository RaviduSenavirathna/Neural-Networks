# Neural-Networks
Built a neural network from scratch in Python using NumPy, implementing forward propagation, backpropagation, loss calculation, and gradient descent without high-level deep learning frameworks.
